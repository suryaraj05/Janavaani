# Place your Firebase service account JSON here (gitignored).
# Download: Firebase Console → Project settings → Service accounts → Generate new private key
# Then either:
#   - set FIREBASE_SERVICE_ACCOUNT_PATH=infra/serviceAccountKey.json in .env
#   - OR copy client_email + private_key into .env
